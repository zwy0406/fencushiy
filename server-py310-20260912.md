# GA-MP-DCA server environment

- Server: interactive80553
- SSH: `ssh -p 25370 root@49.123.2.32`
- Project: `/root/GA-MP-DCA-main`
- Environment: `/root/miniconda3/envs/py310`
- Python: 3.10.21
- GPU: NVIDIA GeForce RTX 3090, 24 GB
- Driver: 525.125.06

## Activate

```bash
source /root/miniconda3/etc/profile.d/conda.sh
conda activate py310
cd /root/GA-MP-DCA-main
```

## Restore Dependencies

```bash
conda create -n py310 python=3.10 pip -y
conda activate py310
python -m pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu118
python -m pip install -r requirements.txt
```

## Validate

```bash
python -m pip check
python -m unittest discover -s tests -p 'test_mwmp_dca.py' -v
MPLBACKEND=Agg python -u -m experiments.run_experiment --protocol MWMP_DCA --cluster-algorithm GA_MP_DCA --predictor-mode lstm --seed 2025 --n-drones 5 --speed 20 --sim-time-s 2 --output-dir results/server_smoke_small_20260912
```

## Save Before Server Refresh

The 20-drone, 30-second validation run was manually interrupted because the
simulator polls packet queues every 10 microseconds and generates many events.
The smaller smoke scenario checks deployment only, not scientific performance.

The assumed creation date is 2026-09-12. With a 15-day lifetime, the first
expected refresh is 2026-09-27 and the first reminder is 2026-09-24.
Use the provider's actual expiry date if it differs.

Save project code, models, data, results and logs outside the server. Export
the environment using `python -m pip freeze` and `conda env export -n py310`.
The source project remains at `D:\codex\fencu\GA-MP-DCA-main`.

Deployment archive: `D:\codex\fencu\.codex_tmp\ga-mp-dca-server-20260912.tar.gz`.
It contains code, models and data, but excludes historical results, research
reports, manuscript directories and SSH credential directories.
