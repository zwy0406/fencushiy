# Algorithm Revision Plan

## 当前状态

Research Mode 证据闭环已完成，服务器必需检查为 `12/12`，本地包校验通过。综合判定为：

- `overall_decision = algorithm_revision_required_before_paper`
- `paper_gate = closed`
- `server_evidence_complete = true`
- `paper_resume_ready = true`

这表示证据收集已闭环，但算法与实验主张仍需要修订，不能恢复正式论文写作。

## 必须修改

1. LSTM 预测主张必须降级或暂停。

   当前 LSTM checkpoint 在正式风格在线 replay 中未优于运动学基线，且预测接近目标均值先验。后续若继续保留 LSTM，需要使用正式场景轨迹重新训练或重设计，并在相同样本集合上报告米制欧氏 RMSE、运动学基线 RMSE、目标均值基线 RMSE 和在线 replay RMSE。

2. LSTM 指标口径必须拆开。

   离线归一化分量 RMSE 和在线米制欧氏 RMSE 不能混用。后续所有表格和文字必须明确单位、目标空间、聚合方式和样本集合。

3. GAT 必要性主张必须降级或删除。

   当前证据显示训练 GAT 可被非神经启发式、未训练 GAT 或无拓扑替代项匹配。若继续保留 GAT，必须重新设计标签和输入特征，并加入训练 GAT、未训练 GAT、度数启发式、常数基线、无拓扑替代项的端到端消融。

4. TLET 正式配置必须重调。

   当前正式设置下 TLET run-level 平均值接近 horizon 上限，评分贡献接近最大值，且去掉 LET/TLET 项后 E7 的 PDR 和 routing load 均值更优。后续必须重调通信半径、horizon、聚合方式或权重，再重新运行消融。

5. 自修复主张必须保持关闭。

   既有 Research Mode 判定中自修复仍属于高风险区域。除非完成有效故障注入并记录故障事件、恢复窗口、恢复前后路由指标和日志，否则不能声称系统具备已验证的自修复能力。

## 建议修改

1. LSTM 训练目标从绝对位置预测重新评估。

   可以比较绝对位置、相对位移、速度增量或残差预测目标。若保留绝对位置预测，需要加入输出方差、目标均值距离和运动学基线对照作为训练验收门槛。

2. LSTM 在线评估增加同样本基线表。

   每个在线 replay 样本应同时保存 LSTM 预测、运动学预测、目标均值预测、真实未来轨迹、输入窗口长度、是否 padding、RMSE 单位和随机种子。

3. GAT 标签重新设计。

   避免标签直接等价于 `degree_ratio` 和 `future_neighbor_stability` 的手工组合。若目标仍是簇头选择，应让标签来自真实端到端收益、稳定性代价或多目标 Pareto 结果，而不是已有评分项的重写。

4. GAT 特征审计常态化。

   加入邻接扰动敏感性、特征扰动敏感性、训练前后差异、分数多样性和负能量特征检查。若训练 GAT 无法稳定超过未训练 GAT，应自动阻止强学习主张。

5. TLET 从 run-level 平均改为更能反映链路风险的统计量。

   可评估 node-min、低分位数、链路 pair 分布、TLET margin 或相对 horizon 的风险距离。正式权重应通过敏感性扫描确定，而不是直接使用当前饱和设置。

6. 实验门槛加入“可替代性检查”。

   对 LSTM、GAT 和 TLET 均设置简单基线。如果复杂模块不能超过相同样本上的简单基线，论文只允许报告负结果或消融观察。

## 可以删除

1. 删除或暂停当前 LSTM checkpoint 的正向性能 claim。

   不要求删除代码，但在重新训练前不应把该 checkpoint 作为正式性能提升证据。

2. 删除强 GAT 必要性叙事。

   如果后续替代实验继续显示启发式或未训练 GAT 等效，可以从最终算法描述中删除神经 GAT 组件，改用透明启发式评分。

3. 删除当前正式配置下的 TLET 有效性 claim。

   如果重调后 TLET 仍不能改善端到端指标，可以从最终评分函数中删除 LET/TLET 项，或仅作为风险诊断指标保留。

4. 删除未验证的自修复 claim。

   如果没有有效故障注入证据，应从论文贡献、摘要和结论中移除自修复相关表述。

## 不能修改

1. 不能修改已冻结的正式结果。

   `/root/FANET-MWMP-DCA/results/formal_study_20260709` 中的正式 CSV、聚合指标和图表只能作为历史证据引用，不能回写或修饰。

2. 不能在当前阶段修改 GA-MP-DCA 核心路由逻辑。

   本阶段目标是根因定位和修订计划，不是直接改算法。后续若进入修订实验，必须另开可追踪实验分支，并保留原始正式结果。

3. 不能把 Research Mode 诊断输出改写成正式实验结果。

   Research Mode 输出只能作为诊断证据、风险评估和修订依据。若要形成正式指标，必须重新运行受控实验并保存命令、参数、随机种子、日志、原始 CSV、聚合结果和图表。

4. 不能混用不同单位和样本集合。

   LSTM 的归一化分量 RMSE、米制欧氏 RMSE、静态样本 RMSE、在线 replay RMSE不能在同一结论中直接比较，除非明确转换和样本一致性。

5. 不能恢复论文写作。

   在 LSTM、GAT、TLET 和自修复风险未完成修订或降级前，论文 gate 保持关闭。
